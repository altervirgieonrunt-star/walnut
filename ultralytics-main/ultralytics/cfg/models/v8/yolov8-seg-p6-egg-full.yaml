# Ultralytics 🚀 AGPL-3.0 License - https://ultralytics.com/license

# YOLOv8-seg-p6-egg-full: 鸭蛋分割专用完整改进版
# 集成所有改进：P6 + Waterfall ASPP + ColorShapeAttention + DCNv2 + C2f_DCN
# 适用场景：俯拍白色鸭蛋在土色背景中的精确分割

# Parameters
nc: 2  # number of classes (egg, barrier)
scales: # model compound scaling constants
  # [depth, width, max_channels]
  n: [0.33, 0.25, 1024]
  s: [0.33, 0.50, 1024]
  m: [0.67, 0.75, 768]
  l: [1.00, 1.00, 512]
  x: [1.00, 1.25, 512]

# YOLOv8-P6 backbone with improvements
backbone:
  # [from, repeats, module, args]
  - [-1, 1, Conv, [64, 3, 2]]  # 0-P1/2
  - [-1, 1, Conv, [128, 3, 2]]  # 1-P2/4
  - [-1, 3, C2f, [128, True]]  # 2
  
  - [-1, 1, Conv, [256, 3, 2]]  # 3-P3/8
  - [-1, 6, C2f_CSA, [256, True]]  # 4 - 使用ColorShapeAttention
  
  - [-1, 1, Conv, [512, 3, 2]]  # 5-P4/16
  - [-1, 6, C2f_CSA, [512, True]]  # 6 - 使用ColorShapeAttention
  
  - [-1, 1, Conv, [768, 3, 2]]  # 7-P5/32
  - [-1, 3, C2f_DCN, [768, True]]  # 8 - 使用DCN（深层特征）
  
  - [-1, 1, Conv, [1024, 3, 2]]  # 9-P6/64
  - [-1, 3, C2f_DCN, [1024, True]]  # 10 - 使用DCN（深层特征）
  
  - [-1, 1, ASPP_Waterfall, [1024]]  # 11 - Waterfall ASPP多尺度感受野

# YOLOv8-P6 head with improvements
head:
  - [-1, 1, nn.Upsample, [None, 2, "nearest"]]
  - [[-1, 8], 1, Concat, [1]]  # cat backbone P5
  - [-1, 3, C2f_DCN, [768, False]]  # 14 - Neck使用DCN
  
  - [-1, 1, nn.Upsample, [None, 2, "nearest"]]
  - [[-1, 6], 1, Concat, [1]]  # cat backbone P4
  - [-1, 3, C2f_DCN, [512, False]]  # 17 - Neck使用DCN
  
  - [-1, 1, nn.Upsample, [None, 2, "nearest"]]
  - [[-1, 4], 1, Concat, [1]]  # cat backbone P3
  - [-1, 3, C2f_CSA, [256, False]]  # 20 (P3/8-small) - 浅层使用CSA
  
  - [-1, 1, Conv, [256, 3, 2]]
  - [[-1, 17], 1, Concat, [1]]  # cat head P4
  - [-1, 3, C2f_CSA, [512, False]]  # 23 (P4/16-medium) - CSA
  
  - [-1, 1, Conv, [512, 3, 2]]
  - [[-1, 14], 1, Concat, [1]]  # cat head P5
  - [-1, 3, C2f_DCN, [768, False]]  # 26 (P5/32-large) - DCN
  
  - [-1, 1, Conv, [768, 3, 2]]
  - [[-1, 11], 1, Concat, [1]]  # cat head P6
  - [-1, 3, C2f_DCN, [1024, False]]  # 29 (P6/64-xlarge) - DCN
  
  - [[20, 23, 26, 29], 1, Segment, [nc, 32, 256]]  # Segment(P3, P4, P5, P6)

# 改进说明：
# 1. P6架构：增加大目标检测能力，适合俯拍大场景
# 2. Waterfall ASPP：多尺度感受野，捕捉不同大小的蛋和蛋群
# 3. ColorShapeAttention：浅层和中层使用，捕捉颜色对比（白蛋vs土色）和椭圆形状
# 4. C2f_DCN：深层使用，可变形卷积适配椭圆边界和不规则蛋群轮廓
# 5. 策略：浅层关注颜色+形状，深层关注精确边界

